# project

## Start

> Prerequisites:
> - docker, docker compose

1. Configure environment
```
cp .env.local .env
vim .env
```

2. Build images and start
```
docker compose build
docker compose up -d
```
